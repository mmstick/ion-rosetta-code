# http://rosettacode.org/wiki/FizzBuzz

for i in {1...100}
    if test $((i % 15)) -eq 0
        echo FizzBuzz
    else if test $((i % 3)) -eq 0
        echo Fizz
    else if test $((i % 5)) -eq 0
        echo Buzz
    else
        echo $i
    end
end
